apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: https-route-pdf
spec:
  hostnames:
    - pdf.k8s.prrlvr.fr
  parentRefs:
    - name: cilium-eg
      namespace: cilium
      group: gateway.networking.k8s.io
      kind: Gateway
  rules:
    - backendRefs:
        - name: pdf-tools-service
          port: 8080
